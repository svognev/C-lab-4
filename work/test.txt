countdown (счет в обратном порядке). Эта программа считает в обратном порядке, начиная с какого-либо 
преобразуется в целое значение с помощью стандартной функции atoi(). Если вторым аргументом командной 
строки (а если считать аргументом имя программы, то третьим) является строка «display» (вывод на экран), 
Другим небольшим примером использования аргументов командной строки является приведенная далее программа 
программы; argv[1] указывает на первый аргумент и так далее.

то результат отсчета (в обратном порядке) будет выводиться на экран
значения (указанного в командной строке), и подает звуковой сигнал, 
argv[0] указывает на первую символьную строку, которой всегда является имя 
Пустые квадратные скобки указывают на то, что у массива неопределенная 

когда доходит до 0. Обратите внимание, что первый аргумент, содержащий начальное значение, 
Теперь получить доступ к отдельным аргументам можно с помощью индексации массива argv. Например, 
